import { TimeUtil, MutationButton, GlobalProvider, AntdForm } from 'dubhe'

export default function App() {
    return (
        <GlobalProvider>
            <AntdForm fields={[]}></AntdForm>
        </GlobalProvider>
    )
}
